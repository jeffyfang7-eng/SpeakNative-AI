# 用Google AI Studio开发APP

先自己写一个提示词，描述APP的一些场景需求以及模块等。然后把初步的提示词发给AI，在让AI帮我润色提示词

提示词：我需要搭建一个英语口语学习APP,需要有日常对话场景,AI地道回复润色,AI外教陪练功能
       请帮我基于上述需求,从专业程序员的视角帮我完善prompt，然后在开发


AI帮我开发完成后下面就是调试


完成后点击右上角的设置->Github 上传，然后自己下载到本地，进入本地该项目内，修改.env.example里的GEMINI_API_KEY

然后命令行在该项目目录下安装依赖: npm install
启动：npm run dev    http://localhost:3000

-----------------------------------------------------
APP做好后如何部署到手机上？

有一个要注意的就是该项目需要掉Gemini的AI功能，目前项目是前端项目，所以API KEY写在前端不安全，所以如果要想安全就得把KEY保存在后端，这就需要AI帮我们把项目改造成前端+后端的项目，我目前没改造。可以这么写提示词：
   
   把这个项目改造成一个NextJs的前后端应用,注意GEMINI_API_KEY要安全的存放在后端,在后端调用AI功能,确保我的APIKey不会泄露

改造完成后下在本地测试然后测试，新建.env.local文件里面写GEMINI_API_KEY=？   本地没问题了那么就在部署到netlify这种服务器上
部署上去后确定不报错了，然后下面增加一个环境变量GEMINI_API_KEY， 这个时候就相当于你用服务器上配置的环境变量了，不用刚才.env.local里面写的环境变量了，并且删除  


1.项目传到github
2.把项目部署成域名网站
  https://www.netlify.com/  点击右上角使用github登录，Import a Git repository选github，
  选择你要的项目，添加一个环境变量GEMINI_API_KEY，value随便填（实际本项目是AI项目不要随便填，但是现在安全不是重点，主要学习部署成一个网页版的APP），点击部署。变成绿色后点击tangerine-fairy-6cd25d.netlify.app，就能访问了，手机上敲这个地址也能访问
3.在safari中打开上面刚部署好的页面，然后 “添加到主屏幕” 
4.下载Github客户端，客户端装好后克隆项目到本地，然后vscode打开当前的项目目录，然后改造项目，
  为了底下在push到git上面这样就可以自动部署到netlify了
5.把网页web项目变成PWA的Web应用，这样就看不出来网页的痕迹了。PWA就是披着APP外衣的网站。
  安装PWA插件到Vite项目，在本项目里面打开终端输入：npm install vite-plugin-pwa --save-dev
6.新建一个public文件夹，找一张合适的图片作为App的封面。提示词：帮我做一个 “英语口语学习”  的APP封面图，要求是正方形
  进入claude code 提示词：使用vite-plugin-pwa,把这个项目改造成pwa应用,应用图片在public录下面
7.改造成功后，在github客户端里面输入：改造成PWA应用， 然后点commit to main, 在之后点右侧的 Push origin
8.然后netlify网站会自动同步更新本项目github仓库里的最新代码并部署到最新版本。以为在第2步时两边就绑定了。
  然后删除手机中的之前的web APP，打开手机浏览器，重新访问本项目的网址后再次 “添加到主屏幕” 图标就出现了。
    